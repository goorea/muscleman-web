import { Container } from '@mui/material';
import React from 'react';

type P = unknown;

const TrainingsScreen: React.FC<P> = () => {
  return (
    <Container component="main" maxWidth="xs">
      Trainings Screen
    </Container>
  );
};

export default TrainingsScreen;
