import Container from '@mui/material/Container';
import React from 'react';

type P = unknown;

const LoginScreen: React.FC<P> = () => {
  return (
    <Container component="main" maxWidth="xs">
      Login Screen
    </Container>
  );
};

export default LoginScreen;
